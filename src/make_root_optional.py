#!/usr/bin/env python3
from pathlib import Path
import subprocess, sys
ROOT=Path(__file__).resolve().parents[1]
# Optional placeholder: CSV files are primary. Users can convert with ROOT later.
print('ROOT conversion is optional. CSV datasets are ready in results/.')
